/**
 * SWC Loader Entry Point
 * This file loads the server - transpilation handled by --import flag
 * Required for React Email templates to work
 */

// Import the main server (transpilation handled by Node.js --import flag)
import './server.js';
